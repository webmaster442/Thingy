<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpfext="http://github.com/webmaster442/AppLib/WPF"
                    xmlns:local="clr-namespace:Thingy.DataTemplates">
    <DataTemplate x:Key="ToDoItem">
        <CheckBox
                Grid.Column="0"
                IsEnabled="{Binding IsCompleted, Converter={wpfext:NegateConverter}}"
                IsChecked="{Binding IsCompleted}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Grid.ColumnSpan="4"
                    FontSize="16"
                    TextWrapping="Wrap"
                    Text="{Binding Content}"/>
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    Text="Due: "/>
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    Text="{Binding DueDate}"/>
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="2"
                    Text="Completed: "/>
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="3"
                    Text="{Binding CompletedDate}"/>
            </Grid>
        </CheckBox>
    </DataTemplate>
    
</ResourceDictionary>